import React from 'react';
import { StyleSheet, Text, View } from 'react-native';
import Button from '../components/Button'; import Card from '../components/Card'; import { AppProject, AppScreen } from '../types';
export default function HomeScreen({ project, go }: { project: AppProject; go: (s: AppScreen) => void }) { return <View style={styles.wrap}><Text style={styles.title}>AssetChopper</Text><Text style={styles.sub}>Cut illustrated sheets into transparent PNG assets.</Text><Card><Text style={styles.text}>{project.sourceSheet ? `${project.sourceSheet.name} • ${project.crops.length} crops` : 'No sheet imported yet.'}</Text><Button title="Import asset sheet" onPress={() => go('import')} /><Button title="Open sheet editor" onPress={() => go('editor')} secondary /><Button title="Asset list & names" onPress={() => go('assets')} secondary /><Button title="Export zip" onPress={() => go('export')} secondary /><Button title="Settings" onPress={() => go('settings')} secondary /></Card></View>; }
const styles = StyleSheet.create({ wrap: { flex: 1, padding: 20 }, title: { color:'#fff', fontSize: 36, fontWeight:'800', marginTop: 24 }, sub: { color:'#9fb0c4', marginVertical: 8, fontSize: 16 }, text: { color:'#dbe7f5', marginBottom: 12 } });
